import random

# A library called random is used to generate a random integer with the function random.randint()
# the random.randint() function generates a random integer between a and b which we can input in that order. Once inputted out IDE will show us which is which
number = random.randint(0, 100)
print(number)

userInput = int(input("Enter a number between 0 and 100: "))
print(userInput)
